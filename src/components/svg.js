import React from "react";
import SvgIcon from "react-native-svg-icon";

const Svg = (props) => <SvgIcon {...props} />;

export default Svg;

// how to use
// import this component
// import SVG sprite you want to use
{
  /* <Svg svgs={svgs} name="Adventure" /> */
}
// use sprite for `svgs` props and call svg name
